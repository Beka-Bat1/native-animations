import React from "react";
import { StyleSheet, Text, View, Animated } from "react-native";

const Card = ({ panHandlers, cardStyle, imageStyle, text, image }: any) => {
  return (

  );
};

export default Card;

const st = StyleSheet.create({

});
